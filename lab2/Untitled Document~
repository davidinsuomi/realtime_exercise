#include <unistd.h>
#include <fcntl.h>
#include <sys/types.h>

int main()
{
     int fd,i=0;
     char buff[5]="AAAA"
     fd=open("exlog.txt",0_CREAT|O_RDWR,0700);
     for(i;i<1000000<i++)
     write(fd,buff,4);

     colse)(fd);
     return 0;
}

